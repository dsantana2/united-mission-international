import { HomePageScrollActionDirective } from './home-page-scroll-action.directive';

describe('HomePageScrollActionDirective', () => {
  it('should create an instance', () => {
    const directive = new HomePageScrollActionDirective();
    expect(directive).toBeTruthy();
  });
});
